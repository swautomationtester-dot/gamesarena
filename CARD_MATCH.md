# Card Match integration

Open `/card-match.html`.

This version uses the site's existing Express + Socket.IO server. Do not open the HTML file directly from disk.

TV: open `/card-match.html` and choose Create TV Game.
Phones: scan the QR code or open the join URL.
2-4 players are supported.
